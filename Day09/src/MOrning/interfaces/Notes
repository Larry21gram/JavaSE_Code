interface 接口
class 类

类与接口属于同一级别

接口表示一种规范
抽象类不能直接创建对象，但是可以使用抽象类的引用
接口类似 ，不能直接创建对象，但是也可以使用接口引用 。
区别：接口只能有抽象方法。


 implements ： 实现
   关键字： 实现WatchDog与Work关联，于是WatchDog满足Work的规范。
 一个类可以实现多个接口，多个接口间逗号分割。
 接口由类实现，那么类就拥有接口的规范， 可以把类的对象赋给 接口的引用。(即实现多态) 
 Work w = new WatchDog();
 
 接口规范什么？ 怎么进行规范？
 规范什么就写在接口里，包括： 方法（都是抽象方法）  \ 接口内的属性只能是常量。
 
匿名对象：　没有引用的对象，即没有名字
如：new Car() ;
特点： 生命周期，只能完成一次使用。